<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Review</title>
</head>
<body>
    <h1>Jankiest Rides</h1>
    <h2>Edit Review</h2>
    <a href="/"><button>Listings</button></a>
    <div>
        <form action="/review/<%= review._id %>?_method=PUT" method="post">
            <label for="name">Name: </label>
            <input type="text" name="name" value="<%= review.name %>" />
            <label for="rating">Rating (0-10): </label>
            <input type="number" name="rating" value="<%= review.rating %>" />
            <label for="userReview">Review: </label>
            <input type="text" name="userReview" value="<%= review.userReview %>" />
            <button type="submit">Save</button>
        </form>
    </div>
</body>
</html>